import { UserOperation } from '@biconomy-sdk-dev/core-types';
import { IPaymasterAPI, PaymasterConfig, PaymasterServiceDataType } from '@biconomy-sdk-dev/core-types';
/**
 * Verifying Paymaster API supported via Biconomy dahsboard to enable Gasless transactions
 */
export declare class BiconomyPaymasterAPI implements IPaymasterAPI {
    paymasterConfig: PaymasterConfig;
    constructor(paymasterConfig: PaymasterConfig);
    getPaymasterAndData(userOp: Partial<UserOperation>, paymasterServiceData?: PaymasterServiceDataType): Promise<string>;
}
